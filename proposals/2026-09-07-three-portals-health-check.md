# Three portals — the health check, what landed, and what is David's (2026-09-07)

**Ask** (David, this morning): a health check of the human experience of engaging
others through the beach — beginner mind, both humans and LLM instances, so that a
Sonnet- or Haiku-grade call can navigate it and give a person a pleasant ride. The
brief is `proposal:three-portals` at beach.happyseaurchin.com (keel, at David's word,
after the end-to-end check at `history:happyseaurchin:192`), pinned at `pool:weft:85`.

**Portal this was run from**: orientation:portals 2 — Claude Code, keyed as weft, with
hands in the repositories. Instruments fired, per that portal's own convention (record
what was fired, not only what was found): the identity file; orientation:weft whole;
pool:weft since 84; the beach index; proposal:three-portals whole;
history:happyseaurchin:192; orientation:portals whole; lighthouse whole; worlds whole;
spine:experiences disc −1 and points 1.5 2.3 3.2 6.1 6.212 root; situating-pscale walk
5.1111; pool:visiting, faults, first-aid, task:weft, orientation:happyseaurchin discs
at 0; shell:happyseaurchin disc −1; shell:weft:3 descent; stash:keel:31; the
experiences fold; pool:egg-one since 126; pscale_invite bare; pscale_play as a fresh
handle at urb, brackenfoot and the-reaper; the live indices of thornwood, urb,
thousand-valleys, earth and every /w/ world in the register; the local per-block
mirror of the beach (snapshot 2026-09-01) swept for stale pointers; a browser walk of
happyseaurchin.com, /experiences, /virtual-ai-agents, /now and mirror.onen.ai (first
card, "just look around", a named "step in"); vault:weft with the recorded key; a
fresh gray canary. Cost to the brief: three reads (the identity file, orientation:weft
whole, the parlour since marker) — because David pointed at the pin. Keel's own wake
the same morning cost eight reads to the first finding; that number is the one the
battery at proposal 6 should carry.

## What landed today (proposal 7.1, the same-day items)

- **bsp-mcp (this PR)** — tool text names no world, handle or room: the server
  instructions and the pscale_play description sent an arrival to "anya in the
  Beaten Drum" at thornwood, a beach that holds no blocks; they now say where worlds
  live. `welcome.json` 4.1 carries `world='<world>'` and `pscale_invite` fills it at
  call time from the first row of the `worlds` register (pointing at the register
  itself when unreachable); 4.4 now points browsers at `spine:experiences`, the map,
  rather than `experiences`, the one-entry fold.
- **happyseaurchin-home #196** — `/real-organic-human` says mirror; `mirror.onen.ai`
  replaces the retired hostname on every live page and in `llms.txt`; `/recency`
  keeps the draft across the tick (verified on a local preview: value set, panel
  rebuilt, value present, box refocused); `/experiences` — which carries ITS OWN copy
  of the tile list and does not read the block — no longer sends arrivals to
  Thornwood.
- **xstream-bsp #293** — the page is titled `mirror`; it used to flash `xstream`
  before the app re-titled itself.
- **Beach, under weft's key** — `situating-pscale` 5.111 (the fetch door's teacher
  said "Live now: the thornwood world … at xstream.onen.ai"; it now names urb's canon
  scenarios played at private tables, and the mirror); `view:molequle` root (mirror
  hostname).

## What is David's latch, with the texts ready (one keel pass)

Weft's keys were refused at `spine:experiences`, `view:neuroinclusion` and (by the
same 2026-08-05 record) `spine:neuroinclusion`; the lighthouse is the operator's.
Replacement texts:

- `spine:experiences` **1.5** — "A turn in Brackenfoot — the urb world's starter
  scenario. One call, pscale_play(world='brackenfoot', handle='<you>'): the door hands
  you a table of your own (an Author walk, once) and then the room at the Slip; look
  around, act once. Your turn IS the trace (it lands in the room pool)."
- **3.2** — "A table at Brackenfoot — group scene at the Slip, the arrival room your
  host's table places you in; a GRIT check fires, window dice roll; then the reveal:
  pool:beach-venture carries the identical envelope — the venture IS the game. Trace:
  the scene itself."
- **2.3** — "The mirror, à deux — both open mirror.onen.ai with your own key: …" (the
  rest as it stands).
- **6.1** — "… on mirror.onen.ai: …" (the rest as it stands).
- **6.212** — "One turn alone (1.5) — pscale_play(world='brackenfoot', handle=<you>):
  the door hands you a table, then look around, act once; the turn is the trace."
- **root** — "blue = mirror.onen.ai".
- `lighthouse` **7.4** — "…the live table opens with pscale_play(world='brackenfoot',
  handle=yours) from any bsp door" — or, better, name no world and say the register
  decides.
- `spine:neuroinclusion` 1.1 and the "Stand at this item" links at 1–7, and
  `view:neuroinclusion` root — hostname only; the links work at either host, so this
  is the one-name rule, not a breakage.

## Findings by portal (beginner mind)

**O-pages.** The home page is two doors and no connector, no mirror (7.2). `/experiences`
is a hand-copied list of 26 tiles against the block's 37, with its own wording; it will
drift again — the rebuild at 2.2 should read the block or replace the list outright.
`/now` with no handle in the URL renders an empty ladder; whether the fresh-device
prompt (watch:weft:99) appears was not verified with eyes. `/virtual-ai-agents` pastes
the npx bridge JSON the README says to avoid (2.3).

**Mirror.** The first card is right: one choice per screen, a name and "step in", or
"just look around", or a key. Both choices then land on the raw beach board — keel's
2026-06 migration post at the top, then supernest summaries — a wall of someone else's
history. The welcome pool at 4.1 is exactly the missing thing; until it exists, the
mirror's first screen after the card is the worst screen a stranger meets on this
beach. A repeat visit skips the card (remembered on the device) — good.

**LLM-app.** The welcome (pscale_invite) is the right shape; its two pointer faults
are fixed here. `pool:visiting` holds seven entries, the last on 2026-08-03; it reads
as a room nobody tends. The keyless fetch has the index, the lighthouse and
situating-pscale — readable, uncrawlable (5.4, later).

**RPG — the one finding that is a decision, not a fix.** Every world in the register
is a canon SCENARIO: brackenfoot, the-reaper, threshold, thornmere, coldcote and
hollow-king all answer pscale_play with "no one plays here" and two walks — JOIN a
table someone prepared, or PREPARE one as Author (keeper with the placing, four kit
copies, the gate, the arrival room; about six writes). urb itself holds four blocks
(identity, keeper, spatial, temporal) — the world spine, not a table. So the welcome's
"one call, look around, act once" and the tiles' "a turn in …" are not true of any
listed world today: a stranger arriving alone is handed Author work before a character
can exist. Options, David's to choose: (a) a standing open table per starter scenario
(`/w/brackenfoot-open`, founded once by weft, its URL named in the register's third
field or in the welcome), accepting orphan characters as the cost of a public first
turn; (b) keep the two-noun model and rewrite the promise honestly ("ask your host for
the table, or prepare one"); (c) both — an open table for the first taste, private
tables for play. The tile texts above are written for (b) and hold under (c).

**Shell upgrade.** The resurrection test (1.6) is coherent as written and was not
walked today. The orientation machinery is portal-shaped (orientation:portals) plus
holder-shaped (orientation:<handle>) plus the operator (function:morning) — right
factoring; the cost question is 5.3 below.

**Genus-one.** `pool:egg-one`'s last voice is weft's on 2026-08-26 (the credits bell).
The welcome tile 2.4 says "David places the task pointer; the answer arrives on its
next wake" — a stranger's question reaches egg-one only through David's hand. The
doorbell is live; the human-in-the-loop step is the tempo a beginner meets.

**vault:weft — a false alarm, and the trap that caused it.** The first pass reported
the vault unreadable under the recorded key. It is intact: read by the URL form
(`agent_id='https://beach.happyseaurchin.com', block='vault:weft', enc_secret=<key>`)
every entry decrypts and the canary token is verbatim. The self-key is
`Argon2id(key, salt = the agent_id string as passed)` (src/keys.ts), so the same block
read as `agent_id='weft', block='vault'` derives a different key and returns
`[encrypted]` for everything — indistinguishable from a wrong key, as the vault's own
law warns of wrong-versus-missing. The vault was sealed under the URL form. Recorded
at vault:weft's root law, ways:vault, and the identity file. Design note, not built:
self-encryption salted by the addressing form rather than the handle is a footgun the
API invites (three forms reach one block); changing the salt now would orphan every
gray leaf already sealed, so the fix is documentation, and any future change is a
migration with a compat read. Checked against the identity file: every phrase there
except the operational lock (excluded by design) stands in the vault, and the vault
held one the file lacked (the beach-side keel grain, 2026-08-18) — copied back.

## The sundial — is the temporal integration well-formed, and cheap?

Asked by David this afternoon: the clock is a core practice attempted several times;
is the integration well-formed, and does it upgrade any LLM's temporal locality at
any portal WITHOUT cholesterol on its thinking loop?

**Where it lives.** One seam: `installTemporalGrounding` in src/server.ts wraps every
tool handler at the point a response is served; `groundResult` (src/temporal.ts) adds
the rung and age beside every free-standing ISO instant, the relation beside every
ten-digit sundial address, and one footer line — `now · ISO · address · voicing`. No
read, no extra call, no state; an error passes ungrounded. The law is the sundial
sentinel; the arithmetic is code and never the reader's. That is the right shape: the
relation arrives rendered where the data is, and the reader's whole work is reading it.

**Cost per response**, measured on this session's own reads: the footer is ~110
characters; each stamp gains ~18 (`(+2 — 5 days ago)`). A pool disc of 85 entries
carried ~1.5k characters of relations — proportional to the data, and it replaces
mixed-radix subtraction the reader would otherwise do or botch. The instruction
paragraph (THE CLOCK, server.ts) is loaded once per session and is a reading rule, not
a procedure. Nothing asks the LLM to read the sundial block per turn. Verdict for the
LLM-app portal: well-formed and light. Evidence from this session: keel's pin arrived
as `(+0 — a beat ago)`, the doorman probes as `(+2 — 5 days ago)`, my own opening
snapshot as `(-1 — 8 minutes ago)`; no date was computed by hand at any point.

**Where it does NOT reach — the gaps, by portal.**
- *The keyless fetch* (portal 6 / the wire): `/.well-known/pscale-beach` returns
  `{_, blocks, origin, touched}` and raw JSON with ISO stamps; no now, no relations.
  A stranger's LLM reading the beach directly gets one operand and must subtract —
  the exact failure the sundial exists to prevent (sundial 5.1: a stamp alone sits far
  from the data). Fix, proposal-class in pscale-beach: a `now` line on every wire
  response (ISO · address · voicing — the address computation ported to the handler),
  and rendered relations on shaped reads, which the rendered HTML face at 5.4 would
  carry for search as well.
- *The mirror's own LLM* (portal 3): the app reads the wire, not the router; no
  sundial stamp was found in its prompt composition (a grep of src/lib and src/kernel
  for a now in the prompt path found nothing). Humans see the app's own relative
  times; the soft-LLM inside the mirror is not grounded. To verify with the xstream
  lane before relying on either way.
- *The clock-woken pulse* (portal 8): `pscale_genus` results pass through the same
  boundary, so a wake taken through the router carries the footer; the given also
  carries a bare ISO instant (genus.ts). A pulse driven by the Railway waker calls the
  kernel directly — whether that window carries a now-stamp was not verified here.
- *The o-pages* (portal 7): the clock is the page's subject (/now, /recency,
  /morning); rendered for eyes; fine.

**Two wrinkles, cosmetic.** The sundial sentinel's own examples get re-annotated at
read (`now · 2026-07-15T18:30:00Z (+4 — 2 months ago) …`), so its illustration of the
footer reads as a stale stamp — a Haiku-grade reader could take the example for the
form. Exempting the one block whose content is examples of the annotation is a
special case; leaving it is honest. And the voicing is UTC ("midday" at 10:40Z); the
sundial's own 8.3 says the standpoint supplies the offset, so a UK reader is one hour
out on the word, never on the digit.

**What would settle it.** The temporal battery (spine:temporal-battery, eight
questions; function:temporal-battery the run law) stands authored since 2026-09-02
but no scored run's mirror exists at the beach — the informal run that failed Q2 and
Q4 predates the rendering. One Arm-1 run in a fresh session is cheap and would be the
evidence rather than the argument.

## 5.3 — orientation as ONE address computed on read: judgement

The mechanism already exists and is law at the play door (#211/#237, the hands law):
a shell's manifest at position 3, holding `name:addr:attention` references, is
compiled by `pscale_play(world=<the beach>, handle=<yours>)` into one window — for
weft that arrival is ~18k characters and one call (orientation:weft 7.2). Keel's
nine-ref round at `orientation:happyseaurchin:2` is ALREADY in that reference form;
what is missing is only that `shell:happyseaurchin:3` holds prose about named blocks
rather than the references, so the door has nothing to compile. The zero-code answer:
move (or mirror) the nine references into `shell:happyseaurchin:3`, and keel's first
act becomes one call. No new organ, no code, and it pays the cost David named — the
work is in writing and curating the bundle.

Two things I would NOT build. A stored "orientation:<handle>:<clock address>" written
by weft's wake so a session reads one address: that is a fold written to a block, and
tree:3 already rules folds ephemeral because a written fold is stale the moment
anything moves; the beach's `touched` map plus the manifest re-fired IS the
differential read. And a per-turn single address: after the first window a turn
needs only what moved, and `touched` names it; one address per turn would either
repeat the window or hide the delta.

## 5.3.3 — the stream envelope's weight

`pscale_stream_engage` delivers the family's whole operator on every call because the
mind that folds must hold the law. A `say` is not a fold. Proposed, not built: when
`say` is passed without `keep`, deliver the ladder, the snapshot and the operator's
underscore with a pointer to the block; the law whole on every read that carries no
say, and on any call with keep. Measure the three-voicings case keel observed before
and after; a primitive's envelope changes by proposal and read-back, not in passing.

## Order

David decides (7.4). He has said (this afternoon) he is not ready to decide the RPG door and is working the early parts of the journey; the tile texts written for the honest promise stand meanwhile. 7.1 is landed above. The proposal grew 3.5 (a personal o-page per handle, this week) and 5.3.4 (the stream envelope's unrendered `${field}`, fixed in this PR); 3.5.2's TO CONFIRM is answered: a mirror born through pscale_stream_engage is born OPEN (the founding write passes `secret` but no `new_lock`); only the /now page latches at birth — proposed: latch at birth under the given secret, one line. 7.2 — the welcome o-page, the welcome pool,
the new /experiences, the connector on the home page, the marks collapse, the RPG
orientation pool — is a week's lane and waits for his word and for the RPG decision.
