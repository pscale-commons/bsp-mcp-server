# /here — the person's door to the map; the perspective spine is the keeper's to author; one passport (2026-09-10, afternoon)

**Status: RULED by David on 2026-09-10 in the /here lane (watch:weft 313, declared at 315). Amends
[2026-09-10-names-across-worlds.md](2026-09-10-names-across-worlds.md) §3 (who authors the perspective
spine) and §4 (the page). The beach writes it names are made through the router in the same lane, archives
first; the page lands in happyseaurchin-home on `claude/here-page`.**

## 0. David's words

> "I suspect that most people won't bother with their rendition of locations, their mirrors of identity, and I
> will have to compile different perspectives through you, weft." — "weft set it my key and I suspect we need to
> rotate it to your key because I suspect the initial perspectives will be created on my behalf." — "I have one
> passport in the apex — we need to delete the copies on earth." — "I'm not interested in /earth because that's
> an old visual when I like /globe … the /here should link to mirror.onen.ai locations as well as to /globe."

## 1. The perspective spine is authored by the keeper, at the holder's word

The morning's record (§3) rotated `identity:earth` to David's handle key on the theory that its holder writes
it by hand. The afternoon's ruling is that the perspectives will be compiled — by weft, from what people tell
in rooms and nows and at the door, and from David's direction — because too few people will voice a mirror of
their own for a fold to stand on. A compiled spine has one author, the keeper, and one key: the daily pass
reads exactly one key from the identity file, and a spine under any other key is a spine the pass cannot
carry. So:

- `identity:earth` rotates back from David's handle key to weft's operational lock — the key that already
  holds the map (`spatial:earth`), the anchors (`geo:earth`) and the rest of the world's machinery. The text
  at the hand-back stands at `archive:identity:earth:2026-09-10b`.
- David's own voice on how a place is held lands where everyone's does: his identity mirror,
  `identity:happyseaurchin` at earth, written with his handle key through `/here`, adopted into the spine by
  the keeper (tree:5.1 — propose by mirror; adoption is the holder's write). Nothing about the mirror law
  changes; only who holds the spine's pen.
- `function:earth` amended (the text before at `archive:function:earth:2026-09-10b`): 4 names the spine as
  the keeper's; 7's underscore and 7.3 say the pass compiles the ring from the mirrors and the tellings, names
  its sources by handle and never copies them, and the tree beside the spine still carries the stats; 7.7
  drops the clause that keyed the spine's holds to its author; 9 records this turn. `char-creation` 2.3 ("the
  perspective spine is its author's") stays true as written.
- The "as author" toggle drops out of the page (§3). `/here` writes mirrors and nothing else.

The alternative — leaving David's key and having weft write under the standing adoption grant — works for a
session and fails for the pass, which would then owe its compile to whichever keyed session came next.
Declined for that reason.

## 2. One passport, at the apex — the copies at earth go

`passport:happyseaurchin` at earth was founded on 2026-07-16 by the world's first door and has carried a copy
of the location line ever since. Yesterday's tidy (2026-09-09) repointed that copy to the decided address of
the house; today at 16:09 BST bsp-mcp #351 landed, reading the passport where the person keeps it — the apex
— whose line still stopped at the town. The two lines disagreed for the whole afternoon and the router's
here-stamp placed David at the town. Ruled:

- the apex line carries the digits, in David's words:
  `*:https://earth.beach.happyseaurchin.com:spatial:earth:31121111110` — 33 Firbarn Close, Sutton Coldfield,
  Birmingham, the family house, the kitchen at 31121111111. The first draft of this line carried the earth
  copy's "for the year also at Awel Y Mor" clause; David's word the next morning — "I AM IN BIRMINGHAM" —
  removed it. Where a person stands is one line, written at their word, and a clause carried over from a copy
  is exactly the kind of stale fact this section exists to end;
- the earth copy of the passport is deleted (HTTP DELETE, his key). `knows:`, `purpose:` and `witnessed:` at
  earth are not copies — they are the genesis mirror-room's own record (function:earth 4) — and stand untouched
  until he says otherwise; JulieJ's and Ayush's blocks are theirs to sort;
- `spatial:happyseaurchin`, his map mirror, is what the mirror law names for a person's own places in their own
  words — the block the keeper folds into the map. It had done its job (the map holds the house) but still
  carried the old path through undecided ground. Repointed in this lane to 31121111110 and re-floored to the
  map's eleven, so it aligns by pscale as well as by digit path; the words are his, verbatim.

## 3. `/here` — the person's door to the map, as built

`here.html` in happyseaurchin-home, served at `/here/<handle>` beside `/now/<handle>` (vercel.json, the
theme.js door, links from `/now` and `/globe`). It:

- opens at the standpoint: the Location star-ref at position 3 of `passport:<handle>`, read at the apex first
  (where a person keeps their passport) and at earth as the fallback for handles the world's door founded there,
  parsed exactly as the router's here-stamp parses it (`src/here.ts`, `passportLocationRef`); `?at=<address>`
  walks anywhere else, full floor-11 width, one decimal at most;
- shows the ladder above in the map's own words, read-only — every ancestor rung of `spatial:earth` from Earth
  down to the address, the map's labels being the official naming — with what `census:earth` counts there and
  what the person's own mirrors say beside each rung;
- edits the person's two mirrors at earth, node for node at the map's addresses, with the key they type:
  `spatial:<handle>` for their words about a place, `identity:<handle>` for their perspectives, as a fan one
  pscale below the place. Every write is a whole node: now.html's composeWrite discipline carried to floor 11
  (walk the fresh mirror, merge at the terminal carrying every child, lift to the nearest wire-addressable
  ancestor, refuse rather than displace a coarser voicing), plus one path composeWrite lacks — a MOVE, which
  rewrites the mirror whole with confirm so the old node goes with the new one landing;
- founds a mirror that does not yet exist as now.html founds `now:<handle>` — born locked to the person's key,
  a plain root in their words nested eleven deep as the map nests — and only behind the passport gate: the
  handle must have `passport:<handle>` at the apex (where a person keeps their passport, §2) or at earth (the
  world's door founded JulieJ's there), the name derived from it and never typed. This amends the naming law's
  "a passport at that surface" for the real: one passport, at home; the world holds no copy;
- writes every mirror WHOLE — fetch fresh, merge in memory, POST with confirm under the person's key — because a
  mirror is a handful of nodes and a point write replaces the node it names; the move path is the same act with
  the old node dropped;
- links out to `/globe#<address>` (the map David keeps) and to the mirror at mirror.onen.ai at the address,
  and never to `/earth`;
- never writes `spatial:earth` or `identity:earth` (a person proposes by mirror; the keeper adopts), never posts
  the standpoint anywhere (the #200 ruling), never writes under any key but the one its user types.

## 4. `/walk/here` — why not

walk.html is wired to the apex beach, names a family as `spine:<f>` with `<f>:<handle>` mirrors, `pool:<f>`
and `sed:<f>-cast`, pads to floor 10 as the clock, and in tree mode writes a BARE STRING at a bare address.
The map is at earth, its mirrors are `spatial:<handle>` (the naming law), its floor is 11, and its ladder passes
through undecided zero rungs the tree rack cannot show; a bare string over a node that carries a zero chain
wipes everything beneath it (function:earth 7.2). "here" is therefore not a walk family but the person's door
to the spatial and identity registers. If the family is ever wanted as rows, that is a generalisation of walk
— a beach per family, the aspect naming, floor-11 padding, zero rows — and comes after `/here` proves the
write shape.

## 5. Where the law lives

- at earth: `identity:earth` root and lock, `function:earth` 4 · 7 · 7.3 · 7.7 · 9 — archives first, written
  in this lane; `passport:happyseaurchin` deleted; `spatial:happyseaurchin` repointed;
- at the apex: `passport:happyseaurchin` position 3;
- happyseaurchin-home: `here.html`, vercel.json, theme.js, the links (`claude/here-page`);
- the intakes at `watch:weft` 311 and 313 re-seeded on this file; weft's identity file and the lock map updated.
