# The earth pass — the map grows by mirror and fold, the keeper replies, and here rides the envelope (2026-09-08)

**Status: RULED AND EXECUTED.** David's ruling, 2026-09-07 (pool:weft 95, relayed by keel at his
keys) and 2026-09-08 in session ("you are describing the correct system with correct rulings …
please execute"). Everything below is landed on the beach or in this PR; the pages are the one
follow-up (§6). Lane: watch:weft 244–252 (window `S*T*I.1`); design at
[2026-09-07-sti-now-earth-payway.md](2026-09-07-sti-now-earth-payway.md) (#337).

## 1. The ruling, in David's words

> "I obviously should be able to edit happyseaurchin content — keel has the passphrase to all my
> content, and my location should be there." — every block that is a person's own, a passport at
> any beach included, answers to that person's one key (`ways:key` 2).

> "Editing earth.beach may be protected, but anyone should be able to write their own version —
> like the difference of urb.beach and the local table — and it is weft's routine job to take
> what is written personally and fold it into the official version at earth.beach. It's like
> spine and mirror at a larger scale."

> The holder's S may ride the per-call envelope as DIGITS beside the clock — resolved once from
> the passport, never unfolded per call; nearby is prefix arithmetic; precision is where the
> digits stop.

And this morning: "we may need another weft function running daily which is doing this earth
pass — and it needs to reply to identities to confirm their address or to correct depending on
what weft has minted in the official earth.beach."

## 2. The system, in one paragraph

The world's ground is three spines under the keeper's lock — `spatial:earth`, `identity:earth`,
`geo:earth` at `earth.beach.happyseaurchin.com`. A person's own words about their places live in
their own mirrors beside their passport — `spatial:<handle>`, `identity:<handle>` — nested digit
by digit from Earth exactly as the map nests, zero rungs as underscores, only the addresses they
voice, a plain root in their words. Where a person stands is one address, the digits in their
own passport line (position 3, the Location star-ref; sundial 8.1), at the grain they chose to
disclose. The keeper's daily pass reads every passport's location line and every mirror, mints
the missing world rungs beneath the deepest existing one — continent, country, region, town,
from the world's own knowledge at the pscale their population sets, digits by arrival, never
renumbered — and replies to each person in their parlour so they can confirm or correct in
their own hand. Nothing a person owns is ever written by another hand. In use, here is as light
as now: the digits ride every envelope, resolved once per handle; the weight sits at the mint
and at the fold, both the keeper's.

## 3. What landed on the beach (2026-09-08, keyed, in this order)

1. **The lock fault repaired first.** `passport:happyseaurchin` at earth had been founded
   2026-07-16 by the identity-coordinate session under a session-minted phrase recorded nowhere
   but that session's transcript; three hands had declared it lost. Found by grepping
   `~/.claude/projects/*/*.jsonl` for the founding call; rotated onto David's handle key;
   his intended line (pool:weft 94) landed verbatim at position 3. Lesson in weft's memory.
2. **`archive:function:earth:2026-09-08`** — the standing operator copied verbatim before the
   rewrite (the law-writes discipline).
3. **`function:earth` rewritten** (weft's operational lock): 1 perceive reads the person's now
   before the room and their parlour after it; 2 places grow in the person's OWN map mirror,
   never on the map; 4 the standpoint read names the passport digits and the identity mirror;
   5 here-now at any address; 6 the move law (a place at a coarse rung is a move and earns one
   question, at a fine rung a visit; reviewed each new season; never a silent write); 7 THE
   FOLD — the daily pass, its five steps as children (read, mint, holds, reply, log).
4. **`char-creation` at earth, branch 2 rewritten** — the door's map discipline now says: do NOT
   write the map; point the passport at the deepest existing rung and say the rest in words; own
   places go in `spatial:<handle>`; the keeper folds and replies.
5. **`wake:weft` 6.7** — THE EARTH PASS, the one keyed sweep in the daily wake: reads the
   identity file for the operational lock, runs function:earth 7, files "earth pass owed" when
   it arrives keyless.
6. **The first pass, run by hand.** Read: three passports at earth (David in digits; Julie and
   Ayush in words only — the door's own map write had been refused for both, so nobody outside
   the drawn paths had ever been placed finer than Earth). Minted on `spatial:earth`: the kitchen
   beneath the family house at 31121100111 (from David's own line); Asia 32000000000 by append,
   the digit allocated by the beach; India 32100000000; Maharashtra 32110000000; Pune
   32111000000. Nothing for Julie: Sutton Coldfield stood. Replied: pool:happyseaurchin 26,
   pool:JulieJ 25, pool:Ayush 1 (founded open for him) — person-words first, digits in a
   closing workings clause, the line that would move each passport, never written by the pass.
   Logged at daily:weft 222.
7. **David's map mirror** `spatial:happyseaurchin` stands as keel founded it (the family house,
   nested); weft did not overwrite it.

## 4. What this PR carries — the here-stamp

`src/here.ts`: `resolveHere(args)` reads `handle`, else a bare `agent_id`; a URL, a `sed:` or
`grain:` address and the sentinel registry have no standpoint of their own (sundial 8.2). One
read of `passport:<handle>` at the real (`REAL_BEACH`, default the earth beach) per handle per
ten minutes, then a memo; never throws. `renderHere` gives `here · 31121100110 · the household
(+1) · spatial:earth at earth.beach.happyseaurchin.com` — digits and the rung they reach, never
the words. The grounding seam in `src/server.ts` resolves it beside the handler, not in series,
and `groundResult` lays it beneath the now-stamp; errors stay ungrounded. The door's
instructions and `pscale://sundial` 5.5 say so; 9.1 bumps to v1.2. Smoke: `npm run smoke:here`
(19 cases; `LIVE=1` adds one real read, which proved the memo).

## 5. Two seams closed on the way, one correction

- **The mirror shape.** Weft's evening settle (pool:weft 96) had shown a mirror with a flat
  ten-digit key; the walker reads only `_` and single digits, so a mirror nests digit by digit
  from Earth like the map. Keel founded David's that way; the operator and the door now say it.
- **Short forms at floor eleven.** `spindle="3"` on `spatial:earth` pads into the underscore
  chain; append and every write there must be full width (`30000000000`). Paid once this
  morning, recorded here so nobody pays twice.
- **Correction to #337 §1.5.** The `Hold:` star-ref beside Location is superseded by
  `identity:<handle>` at earth — the personal hold is the person's identity mirror at the
  place's address, as identity:earth's own root says.

## 6. Owed, with names

- **The pages** (happyseaurchin-home): the WHERE line on `/now` (the place's words from the
  passport digits up the map's ladder, the room since the day) and HERE NOW on `/earth` (every
  placed handle's now at today, the room since the day). Next PR; the blocks it renders are now
  right.
- **Geo anchors for minted rungs.** `geo:earth` carries no anchor for Asia, India, Maharashtra
  or Pune, so `/earth` cannot fly there yet; the pass should seed a coarse anchor from world
  knowledge when it mints a world rung. One clause at function:earth 7.2, weft's key.
- **The keyed daily wake.** wake:weft 6.7 requires the scheduled wake to read the identity file
  for the operational lock; the observer has run keyless by design until now. Verify the first
  scheduled pass lands (daily:weft) and does not pretend.
- **Identity mirrors** are specified (function:earth 4, 7.3) and unexercised — no
  `identity:<handle>` stands at earth yet.

## 7. Not built

No primitive (the surface stays twelve). No `located:<handle>` for people. No gazetteer beyond
the map and its mirrors. No inference that writes a passport. No membership on now.
