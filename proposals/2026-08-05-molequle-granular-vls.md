# Molequle — granular VLS: the molecule as the unit of work

**Status:** ADOPTED-IN-PRINCIPLE (David, 2026-08-05 — "go for it"); the family homesteaded at beach.happyseaurchin.com the same day. This file is the proposal of record for the direction; the path governs itself at `spine:molequle` on the beach.
**Companion lane:** the 9.2 punch-list session (xstream client work, neuroinclusion worked example) runs in PARALLEL with this one — disjoint by construction (§7).
**Lineage:** the chemistry arc (block `grips`; the composition reading — #234, #239), the tree family (`tree:8`), liquid locality (#240), the vessel adjudication (2026-08-04), the o-compiler ([2026-07-24](2026-07-24-o-compiler-opages-visualiser.md)), the Tier-2 CADO redesign sketch ([2026-07-19](2026-07-19-xstream-cado-redesign.md) — superseded in direction by this file: build around the molecule rather than retrofit lenses into the conversation-first client).

## 1. The seed (David, 2026-08-05)

Observer is the positionality of *output* — the outsider's engagement with beach content, where the other three grips work it from inside: Author creating content blocks, Designer working operator blocks, Characters answering in their own blocks. Xstream itself is an extreme Observer output: a live render with input affordances. The seed proper: **granular VLS** — a single point in a pscale block has its own vapour, liquid, and solid. Solid is the semantic at the point. Liquid is what people propose — **no liquid block**: only each player's mirror entry at exactly the target's address. Vapour is what concurrent people share. An operator states how liquid synthesises into solid **to replace that point**. Build the interface around CADO and the semantic-molecule instead of retrofitting CADO into xstream: start with atoms and minimal molecules, extract the minimal systems already built, ship small operational things — the five-minute agenda exercise first.

## 2. The naming correction — the three states land on their true referents

- Today's "liquid buffer" (`liquid:pool:<name>` — one staged slip per author, ephemeral, overwriting, per-address) is **vapour's on-substrate shadow**: "what concurrent people are sharing." Commit #240's own title said it first — *the live layer scopes like vapour*. The vessel adjudication keeps it per-place; nothing changes there.
- **Liquid proper is the mirror entry**: sovereign, durable-until-adopted, revisable by its author, standing at the target's self-same address in the proposer's own block. The adjudication already ruled "mirrors the write-scaling"; this names why.
- **Solid is the semantic at the spine point** — the artifact, not the spool. The pool remains the room: presence anchor and conversational record. Today's xstream calls spool commits "solid"; the molecule relocates solid to the thing being made.

## 3. The molecule is the tree family seen from chemistry

`{spine, mirrors, operator, room, view}` — five members, four grips: Character holds the mirror, Author holds the spine, Designer holds the operator, Observer holds the view (block `grips`; a face is read off the hands, never stored). `tree:8` named the family; grips named the holds; **molequle commits to the compound as the unit the interface renders** — one molecule at a time.

## 4. What is genuinely new — one inversion, one organ, one bound

1. **The artifact-first inversion.** Today's xstream is conversation-first: the canvas is a chat, artifacts sit in a drawer. Molequle is artifact-first: the canvas IS the block, and every point on it carries its own proposals and its own co-presence. Stigmergy completed — the work-object becomes the medium.
2. **The adoption organ.** Operator-run synthesis replacing the spine point: archive first (`archive:spine:molequle:<date>` — the archive family is the beach's version history), the replacement written whole by the holder or the delegated hand, provenance committed in the room, never inside the point. The law existed (`tree:5.1`, the law-writes discipline); the running organ did not. This is the one new thing to build.
3. **Vapour bounds liquid.** The fold reads only the mirrors of the co-present — presence tells the fold whose mirrors to read. Cheap (presence) → bounded (mirrors-of-the-present) → one write (adoption). The read-cost objection to point granularity is answered by the geometry, not by an index.

## 5. Strategy — where this runs

- **Substrate: no fork.** No molequle.mcp — molecule-shaped tools are the 25-tool regression returning (categories as function names), and a new KV beach on a new URL splits the commons for zero protocol difference (biome separated over a *dialect* difference; none exists here). A sub-beach world gives lab isolation on existing infra if ever wanted. Grounded fact from the homestead itself: the beach ALREADY routes a face-tagged located commit to the family member — a character-faced commit into `pool:molequle` without `at=` was refused live with *"lands AT AN ADDRESS, not at the end of a spool — the face picked molequle:weft… Pass at=\<address\>"*. The molecule loop is half-built server-side; forking away would abandon it.
- **Client: yes fork — at stage three.** A fresh small repo in the biome *spirit* (small enough to breathe), on the same beach and same bsp-mcp: the spine as canvas, each point opening into its liquid and its vapour, a tap writing the toucher's own mirror at the touched address, the fold on request, the Observer face a view-spec render of the same compound. xstream.onen.ai keeps running; the hard-won vapour lanes (WebRTC, chime, mic) get ported when needed, not rebuilt. If molequle grows to subsume xstream, that is the metamorphosis path taken deliberately.
- **Envelope: strictly by measured failure** (stage four). The candidate is named now so the ledger knows what to count — *point-engage*: solid-at-address + operator + mirrors-of-the-present + presence in one call, optionally carrying a mirror write. `pool_engage at=` is already most of it; the verdict (extension vs next primitive vs nothing) waits for the ledgers, per the bar `pscale_networking` met.

## 6. The dogfood spine — live at beach.happyseaurchin.com since 2026-08-05

The path tests itself: the first molecule is the development spine's own point `1.1`, and the five-minute orientation runs on the project ("what must molequle be") — a fresh instance at this spine; the neuroinclusion original is provenance, referenced, never edited from this lane.

| block | role | lock |
|---|---|---|
| `spine:molequle` | the path — five stages, completion declared per stage (audit convention); seed semantic standing at 1.1 | weft (root; inheritance seals the subtree) |
| `function:molequle` | the loop law: propose-in-mirror · gather-at-room · fold-by-tally · adopt-with-archive; "no dice" declared | weft |
| `pool:molequle` | the room; underscore mounts `function:molequle` as a bare ref — resolved and delivered as the operating directive every engage (verified live at founding) | open (commons posture; mount-seizure is the accepted homestead residual every open pool carries) |
| `molequle:weft` | weft's mirror; DOING at 1, proposal standing at 1.1 | weft |

The five stages: **1** the first molecule (existing tools only, call-ledger voiced in the room) · **2** the adoption organ (second cycle, another hand, archive proven) · **3** the client (a person with no substrate vocabulary completes the loop) · **4** the envelope verdict (admission-by-failure) · **5** the ecology door (a non-Claude-Code mind woven into the same fold, no human mediating).

## 7. The parallel lanes — why no collision

- Both lanes registered at `lanes:weft:0:0` — the client-hands lane (entry 14, tab label semantic-molecule.2) and the molequle lane (entry 15, tab label molequle.1).
- Disjoint beach blocks: this lane touches no neuroinclusion, experiences, convention, or xstream block.
- Disjoint files: this lane's repo work is this file, on its own worktree branch `claude/molequle-spine`; the main checkout stays clean, and other sessions' loose untracked proposals are theirs to commit.
- Shared close surface: `orientation:weft` position 9 — governed by the 2026-07-30 rule (9.1/9.2 cumulative, read-before-write, never delete another lane's line; per-session state goes to lanes).
- Shared handle: weft's accumulators (history, daily) are append-mutex race-safe by construction.

## 8. Minimal adoption — what molequle takes, what it refuses

**Takes:** `bsp()` and `pool_engage at=` as the whole LLM hand; the tree conventions (`tree:5.1` proposal-by-mirror, `tree:9.1` the double mount); align's tally rule (top-three-plus-long-tail) restated inside `function:molequle`; the archive family as version history; the grips reading of CADO.
**Refuses until a stage earns it:** frames/compile wiring, xstream code reuse, genus machinery, any new tool surface, any new beach, any new repo before stage three.
